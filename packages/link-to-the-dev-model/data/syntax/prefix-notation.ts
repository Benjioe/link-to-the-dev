import type {syntaxTrad} from "../types"

const fr = {
    name: "prefix notation",
    description: "Toutes les transformations (fonctions, opérateurs ...) s'écrivent avec la fonction en premier suivi par leurs paramètres (ex: + 1 2)"
}

export const prefixNotation: syntaxTrad = { fr };